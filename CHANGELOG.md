# 更新日志

## 1.8.2 - 2026-08-05

### 新增

- 新增确定性法律计算引擎：诉讼费（含保全费、申请执行费）、律师费（可自定义档位）、利息（含迟延履行加倍利息）、日期推算、金额转人民币大写五个计算器，在「工具」页独立使用；Agent 写文书时也通过同一引擎获得确定性结果，法律文书的数字不会错。
- 看板新增「待验收」列：Agent 任务执行完毕后自动落入待验收列，你验收通过才算完成；不满意可打回继续处理（「提议→人工确认」闭环）。
- Agent 按法律规则推算候选期限：材料导入时自动识别判决书（提示上诉期届满+15日）、裁定书（+10日）、保全等（+1年续封提醒），以「待确认」身份进入 Dates 视图；你确认才生效，未确认不参与到期提醒（另一提议→确认闭环）。
- 看板任务可直接拖拽换列（待办 / 进行中 / 待验收 / 完成），并支持按关键词、负责人、优先级筛选、一键隐藏已完成。
- 案件阶段支持补记大事记：推进阶段时随手记录本阶段事项，事后也可点击阶段点补记；阶段列表支持自定义增删与重命名，不再绑定固定八阶段。

### 改进

- Dates 到期表达：逾期与 ≤7 天用信号红 + 倒计时文本（逾期 N 天 / 还剩 N 天），8–30 天灰色倒计时。红只留给到期与逾期，其余沿用圆珠笔蓝设计语言。
- Dates 月视图随窗口宽度自适应，周五至周日的列不再被横向裁剪。
- README 按产品理念重写：工作台结构扩展为五个页面（含工具页），案件驾驶舱、待验收列与待确认期限均有介绍，并重拍全套截图。
- 内置 Subagent 现在作为单一条目编辑和恢复默认，不再需要手动管理覆盖副本。

## 1.8.1 - 2026-08-03

### 新增

- 新增案件驾驶舱：诉讼案件具备接案至结案的八阶段流程线、阶段历史和推进／回退／撤销操作。
- 案件页整合任务、时间线、文档、期限与日程风险，并在首页提供近期优先事项概览。

### 改进

- 案件工作台统一为圆珠笔蓝纸感卷宗视觉：任务、风险、时间线、交付物和文档使用归档式列表与响应式布局。
- 交付物状态可在文档视图中推进，避免在多个视图重复展示同一份清单。

## 1.8.0 - 2026-07-31

### 改进

- 全新「纸感」视觉：暖纸底色与纸纹颗粒，圆珠笔蓝取代信号红成为全局强调色，明暗两主题同步换肤。
- 入口页重设计：出血刊头、竖排档案文字、铜版蚀刻风插画（明暗两套自动切换，带青色重影），输入卡改为蓝边硬投影；夜间主题叠加细扫描线。
- 浮层阴影、警示与成功色统一收编为设计令牌，弹层在各主题下表现一致。

## 1.7.7 - 2026-07-29

### 修复

- Dates 的任务、期限、日程和文件库筛选现在可实际切换；任务继续进入对话，期限和日程改为直接编辑日期或时间。
- 支持将 Vault 原生期限、日程的时间修改写回其 Markdown frontmatter，并保留原有正文。

## 1.7.6 - 2026-07-29

### 改进

- CHANGELOG 现会在每个自然日首次打开首页时自动展示一次；同日安装新版本时也会立即展示对应更新说明。

## 1.7.5 - 2026-07-29

> **⚠ 重要：v1.7.4 及以下版本用户请先手动更新**
>
> 请在终端执行：
>
> ```bash
> npm install -g @tttangerine/mju@latest
> ```
>
> 完成后重新运行 `mju`。升级至本版本后，后续全局安装用户可在 Mju 的 `CHANGELOG` 中检查、确认更新并自动重启。

### 改进

- 在更新日志顶部增加首次手动更新提示，避免旧版用户错过升级路径。
- 界面支持中文／English 切换；对话界面收起模型思考过程，突出律师实际需要阅读的结论与执行结果。
- Vault 任务、Mju 看板、Dates、首页近期在办和任务详情统一为同一条任务链路。

### 修复

- 修复 Vault 任务只在 Dates 显示、却未挂入案件看板和首页的问题。
- 旧版 store 任务会自动迁移到案件 `任务/` 目录；新建、改派、完成或绑定会话均同步回写对应 Markdown。

## 1.7.4 - 2026-07-29

### 新增

- 更新日志现可检查 npm 最新版本；全局安装用户可在 Mju 内确认更新并自动重启。
- 发现新版时会展示对应版本的更新记录；`npx` 临时运行和源码开发会给出安全的重新启动提示，不会执行全局安装。

### 安全

- 应用内更新只允许启动器执行固定的 Mju npm 更新命令；对话和网页均不能传入任意终端命令。

## 1.7.3 - 2026-07-29

### 新增

- 新增版本化 CHANGELOG 弹窗、全局 introduction skill，以及内置子 Agent 的可视化与用户级覆盖机制。
- 重制 μ favicon，并优化对话、思考、工具调用和 Markdown 的视觉层级。

### 修复

- 修复 Windows 项目路径编码：盘符、反斜杠和其他非法文件名字符不再导致初始化时创建 `~/.mju/projects` 目录失败。

### 改进

- Windows 项目 ID 统一分隔符并加入稳定短 hash；既有 macOS/Linux 项目 ID 保持不变。
- 正式版默认端口调整为 `26313`；源码开发服务使用 `30143`，避免与正式办案服务混用。

## 1.7.2 - 2026-07-29

### 新增

- 支持统一材料导入流程：转换完成后自动分析、归类并生成后续复核事项。
- 支持在首页调整默认模型，模型配置保存后会即时刷新可选模型。

### 修复

- 修复 MinerU 返回包的解析、会话启动残影及内置法律子 Agent 的加载问题。
- 移除会触发安装警告的图标依赖，完善 npm 可信发布配置。

### 改进

- 更新 README、发布流程和本地开发说明。

## 1.7.1 - 2026-07-24

### 新增

- 提供默认模型设置、空模型提示及 MinerU 错误诊断。

### 修复

- 对齐源码与 npm 已发布版本。

## 1.7.0 - 2026-07-18

### 新增

- 支持从首页新建案件、统一案件目录，并随包提供默认法律 skills。
- 上线子 Agent 配置界面及内置法律协作团队。

### 改进

- 完善通用本地优先的项目初始化和案卷骨架。
